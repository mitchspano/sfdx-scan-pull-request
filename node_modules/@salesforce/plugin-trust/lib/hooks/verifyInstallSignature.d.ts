import { Hook } from '@oclif/core';
import { ConfigContext, VerificationConfig } from '../shared/installationVerification';
import { NpmName } from '../shared/NpmName';
/**
 * Build a VerificationConfig. Useful for testing.
 */
export declare class VerificationConfigBuilder {
    static build(npmName: NpmName, configContext: ConfigContext): VerificationConfig;
    static buildForRepo(): VerificationConfig;
}
export declare const hook: Hook.PluginsPreinstall;
export default hook;
