import { UX } from '@salesforce/command';
import { Logger } from '@salesforce/core';
import { ConvertResult } from '@salesforce/source-deploy-retrieve';
import { ResultFormatter } from '../resultFormatter';
interface ConvertEntry {
    fullName: string;
    type: string;
    filePath: string;
    state: 'Add';
}
export declare type ConvertCommandResult = ConvertEntry[];
export declare class ConvertResultFormatter extends ResultFormatter {
    protected result: ConvertResult;
    private convertResults;
    constructor(logger: Logger, ux: UX, result: ConvertResult);
    getJson(): ConvertCommandResult;
    display(): void;
}
export {};
