import { UX } from '@salesforce/command';
import { Logger } from '@salesforce/core';
import { ResultFormatter } from '../resultFormatter';
export interface OpenCommandResult {
    url: string;
    orgId: string;
    username: string;
}
export declare class OpenResultFormatter extends ResultFormatter {
    protected result: OpenCommandResult;
    constructor(logger: Logger, ux: UX, result: OpenCommandResult);
    getJson(): OpenCommandResult;
    display(): void;
}
