import { UX } from '@salesforce/command';
import { Logger } from '@salesforce/core';
import { MetadataApiDeploy } from '@salesforce/source-deploy-retrieve';
export declare abstract class ProgressFormatter {
    logger: Logger;
    ux: UX;
    constructor(logger: Logger, ux: UX);
    abstract progress(deploy: MetadataApiDeploy): void;
}
