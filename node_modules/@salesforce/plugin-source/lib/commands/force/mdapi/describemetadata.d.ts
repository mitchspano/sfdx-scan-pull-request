import { FlagsConfig } from '@salesforce/command';
import { DescribeMetadataResult } from 'jsforce/api/metadata';
import { SourceCommand } from '../../../sourceCommand';
export declare class DescribeMetadata extends SourceCommand {
    static readonly description: string;
    static readonly examples: string[];
    static readonly requiresUsername = true;
    static readonly flagsConfig: FlagsConfig;
    private describeResult;
    private targetFilePath;
    run(): Promise<DescribeMetadataResult>;
    protected describe(): Promise<void>;
    protected resolveSuccess(): void;
    protected formatResult(): DescribeMetadataResult;
}
