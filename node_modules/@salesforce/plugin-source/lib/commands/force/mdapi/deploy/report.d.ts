import { FlagsConfig } from '@salesforce/command';
import { MdDeployResult } from '../../../../formatters/mdapi/mdDeployResultFormatter';
import { DeployCommand } from '../../../../deployCommand';
export declare class Report extends DeployCommand {
    static aliases: string[];
    static readonly description: string;
    static readonly examples: string[];
    static readonly requiresUsername = true;
    static readonly flagsConfig: FlagsConfig;
    run(): Promise<MdDeployResult>;
    protected doReport(): Promise<void>;
    protected resolveSuccess(): void;
    protected formatResult(): MdDeployResult;
}
