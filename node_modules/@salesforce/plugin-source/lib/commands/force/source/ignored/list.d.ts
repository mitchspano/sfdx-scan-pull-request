import { FlagsConfig, SfdxCommand } from '@salesforce/command';
export declare type SourceIgnoredResults = {
    ignoredFiles: string[];
};
export declare class SourceIgnoredCommand extends SfdxCommand {
    static readonly description: string;
    static readonly requiresProject = true;
    static readonly flagsConfig: FlagsConfig;
    private forceIgnore;
    /**
     * Outputs all forceignored files from package directories of a project,
     * or based on a sourcepath param that points to a specific file or directory.
     */
    run(): Promise<SourceIgnoredResults>;
    private statIgnored;
    private findIgnored;
    private isIgnored;
}
