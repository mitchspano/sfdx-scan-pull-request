import { FlagsConfig } from '@salesforce/command';
import { FileResponse, RetrieveResult } from '@salesforce/source-deploy-retrieve';
import { SourceTracking } from '@salesforce/source-tracking';
import { SourceCommand } from '../../../sourceCommand';
import { PullResponse } from '../../../formatters/source/pullFormatter';
export default class Pull extends SourceCommand {
    static aliases: string[];
    static description: string;
    static help: string;
    protected static readonly flagsConfig: FlagsConfig;
    protected static requiresUsername: boolean;
    protected static requiresProject: boolean;
    protected readonly lifecycleEventNames: string[];
    protected tracking: SourceTracking;
    protected retrieveResult: RetrieveResult;
    protected deleteFileResponses: FileResponse[];
    run(): Promise<PullResponse>;
    protected preChecks(): Promise<void>;
    protected doDeletes(): Promise<void>;
    protected retrieve(): Promise<void>;
    protected resolveSuccess(): void;
    protected formatResult(): PullResponse;
}
