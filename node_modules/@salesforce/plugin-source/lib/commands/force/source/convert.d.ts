import { FlagsConfig } from '@salesforce/command';
import { ConvertResult } from '@salesforce/source-deploy-retrieve';
import { SourceCommand } from '../../../sourceCommand';
import { ConvertCommandResult } from '../../../formatters/convertResultFormatter';
export declare class Convert extends SourceCommand {
    static readonly description: string;
    static readonly examples: string[];
    static readonly requiresProject = true;
    static readonly flagsConfig: FlagsConfig;
    protected convertResult: ConvertResult;
    run(): Promise<ConvertCommandResult>;
    protected convert(): Promise<void>;
    protected copyDir(src: string, dest: string): void;
    protected resolveSuccess(): void;
    protected formatResult(): ConvertCommandResult;
}
