import { FlagsConfig } from '@salesforce/command';
import { OpenCommandResult } from '../../../formatters/source/openResultFormatter';
import { SourceCommand } from '../../../sourceCommand';
export declare class Open extends SourceCommand {
    static readonly description: string;
    static readonly examples: string[];
    static readonly requiresProject = true;
    static readonly requiresUsername = true;
    static readonly flagsConfig: FlagsConfig;
    protected openResult: OpenCommandResult;
    run(): Promise<OpenCommandResult>;
    protected resolveSuccess(): void;
    protected formatResult(): OpenCommandResult;
    private doOpen;
    private getTypeNameDefinitionByFileName;
    private buildFrontdoorUrl;
    private open;
    private setUpOpenPath;
    private openBrowser;
}
