/// <reference types="node" />
import * as fs from 'fs';
export declare class FileWriter {
    private writeQueue;
    private appendQueue;
    private tmpFilelist;
    private newFiles;
    constructor();
    queueWrite(filepath: string, content: string, options?: fs.WriteFileOptions): void;
    queueAppend(filepath: string, toAppend: string, options?: fs.WriteFileOptions): void;
    writeFiles(): void;
    private revertChanges;
    private removeTempFiles;
    private getHash;
}
