import TypeCache from './TypeCache';
export default class LazyRequire {
    private typeCache;
    private lazyLoader;
    constructor(typeCache: TypeCache, exclusions?: string[]);
    static create(typeCacheFile: string): LazyRequire;
    /**
     * Resets the type cache, whether or not lazy loading has been started.
     */
    resetTypeCache(): boolean;
    /**
     * Start lazy loading type-compatible modules.
     */
    start(): void;
}
