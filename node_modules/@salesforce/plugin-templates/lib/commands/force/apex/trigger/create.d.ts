import { flags } from '@salesforce/command';
import { AnyJson } from '@salesforce/ts-types';
import { TemplateCommand } from '../../../../utils';
export default class ApexTrigger extends TemplateCommand {
    static description: string;
    static examples: string[];
    static help: string;
    static longDescription: string;
    protected static flagsConfig: {
        triggername: flags.Discriminated<flags.Option<string>>;
        template: flags.Discriminated<flags.Option<string>>;
        outputdir: flags.Discriminated<flags.Option<string>>;
        apiversion: flags.Builtin;
        sobject: flags.Discriminated<flags.Option<string>>;
        triggerevents: flags.Discriminated<flags.Array<string>>;
    };
    run(): Promise<AnyJson>;
}
