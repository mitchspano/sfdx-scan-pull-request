import { SourceComponent } from '../../resolve';
import { WriteInfo } from '../types';
import { BaseMetadataTransformer } from './baseMetadataTransformer';
export declare class DecomposedMetadataTransformer extends BaseMetadataTransformer {
    toMetadataFormat(component: SourceComponent): Promise<WriteInfo[]>;
    toSourceFormat(component: SourceComponent, mergeWith?: SourceComponent): Promise<WriteInfo[]>;
    private getComposedMetadataEntries;
    /**
     * Helper for setting the decomposed transaction state
     *
     * @param forComponent
     * @param props
     */
    private setDecomposedState;
    private getDecomposedState;
    private getDefaultOutput;
}
