import { SourceComponent } from '../resolve';
import { ComponentDiagnostic, DeployMessage } from './types';
export declare class DiagnosticUtil {
    private api;
    constructor(api: 'metadata' | 'tooling');
    parseDeployDiagnostic(component: SourceComponent, message: string | DeployMessage): ComponentDiagnostic;
    private parseDefault;
    private parseLwc;
    private parseAura;
    private appendErrorWithLocation;
}
