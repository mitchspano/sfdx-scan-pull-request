import { TestResult } from '../tests/types';
export interface TapResult {
    description: string;
    diagnostics: string[];
    outcome: string;
    testNumber: number;
}
export declare class TapReporter {
    format(testResult: TestResult, epilog?: string[]): string;
    buildTapResults(testResult: TestResult): TapResult[];
    private buildTapDiagnostics;
}
