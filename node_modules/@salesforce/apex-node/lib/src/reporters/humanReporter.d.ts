import { TestResult } from '../tests';
export declare class HumanReporter {
    format(testResult: TestResult, detailedCoverage: boolean): string;
    private formatSummary;
    private formatTestResults;
    private formatDetailedCov;
    private formatCodeCov;
    private formatUncoveredLines;
}
