import { Connection } from '@salesforce/core';
import { JsonCollection } from '@salesforce/ts-types';
export declare function refreshAuth(connection: Connection): Promise<JsonCollection>;
export declare function escapeXml(data: string): string;
